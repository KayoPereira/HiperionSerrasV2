<% content_for :title, "Login | Hiperion Serras" %>
<% content_for :body_class, "login-page" %>

<section class="login" aria-labelledby="login-heading">
  <div class="login__image" aria-hidden="true"></div>

  <div class="login__panel">
    <div class="login__panel-inner">
      <div class="login__logo">
        <%= link_to root_path, class: "login__logo-link", aria: { label: "Ir para página inicial" } do %>
          <%= image_tag "logo-nav-2x.png", alt: "Hiperion Serras", class: "login__logo-image" %>
        <% end %>
      </div>

      <h1 id="login-heading" class="login__title">Painel Administrativo</h1>
      <p class="login__subtitle">Acesse sua conta para gerenciar o conteúdo</p>

      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "login__form" }) do |f| %>
        <div class="login__field">
          <%= f.label :email, "E-mail", class: "login__label" %>
          <%= f.input_field :email, autofocus: true, autocomplete: "email", placeholder: "seu@email.com", class: "login__input" %>
        </div>

        <div class="login__field">
          <%= f.label :password, "Senha", class: "login__label" %>
          <%= f.input_field :password, autocomplete: "current-password", placeholder: "••••••••", class: "login__input" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="login__remember">
            <%= f.check_box :remember_me, class: "login__checkbox" %>
            <%= f.label :remember_me, "Manter conectado", class: "login__remember-label" %>
          </div>
        <% end %>

        <div class="login__actions">
          <%= f.button :submit, "Entrar", class: "login__submit" %>
        </div>
      <% end %>

      <div class="login__links">
        <% if devise_mapping.recoverable? %>
          <%= link_to "Esqueceu sua senha?", new_password_path(resource_name), class: "login__link" %>
        <% end %>
      </div>
    </div>
  </div>
</section>
